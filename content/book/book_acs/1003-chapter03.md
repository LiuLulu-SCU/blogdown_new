# 人群纳排代码



## ACS识别

### 住院库中有ACS主要诊断

```stata
use "文件路径",clear
gen ACS=0
replace ACS=1 if strmatch(cyzd,"*I20.0*")
replace ACS=1 if strmatch(cyzd,"*I21")
replace ACS=1 if strmatch(cyzd,"*I21.*")
replace ACS=1 if strmatch(cyzd,"*I22")
replace ACS=1 if strmatch(cyzd,"*I22.*")
replace ACS=1 if strmatch(cyzdmc,"*冠综*")
replace ACS=1 if strmatch(cyzdmc,"*冠脉综合*")
replace ACS=1 if strmatch(cyzdmc,"*冠状动脉综合*")
replace ACS=1 if strmatch(cyzdmc,"*心肌梗死*")
replace ACS=1 if strmatch(cyzdmc,"*心肌梗塞*")
replace ACS=1 if strmatch(cyzdmc,"*心梗*")
replace ACS=1 if strmatch(cyzdmc,"*不稳定心绞痛*")
replace ACS=1 if strmatch(cyzdmc,"*不稳定性心绞痛*")
replace ACS=1 if strmatch(cyzdmc,"*不稳定型心绞痛*")
replace ACS=1 if strmatch(cyzdmc,"*胸痹*")
replace ACS=1 if strmatch(cyzdmc,"*心痛*")
replace ACS=1 if strmatch(cyzdmc,"*初发劳力心绞痛*")
replace ACS=1 if strmatch(cyzdmc,"*初发劳力性心绞痛*")
replace ACS=1 if strmatch(cyzdmc,"*初发劳力型心绞痛*")
replace ACS=1 if strmatch(cyzdmc,"*恶化劳力*")
replace ACS=1 if strmatch(cyzdmc,"*恶化性劳力*")
replace ACS=1 if strmatch(cyzdmc,"*恶化型劳力*")
replace ACS=1 if strmatch(cyzdmc,"*初发劳累心绞痛*")
replace ACS=1 if strmatch(cyzdmc,"*初发劳累性心绞痛*")
replace ACS=1 if strmatch(cyzdmc,"*初发劳累型心绞痛*")
replace ACS=1 if strmatch(cyzdmc,"*恶化劳累*")
replace ACS=1 if strmatch(cyzdmc,"*恶化性劳累*")
replace ACS=1 if strmatch(cyzdmc,"*恶化型劳累*")
replace ACS=1 if strmatch(cyzdmc,"*自发心绞痛*")
replace ACS=1 if strmatch(cyzdmc,"*自发性心绞痛*")
replace ACS=1 if strmatch(cyzdmc,"*自发型心绞痛*")
keep if ACS==1
destring grbm,replace

```


### 排除陈旧性心梗

```stata
drop if strmatch(cyzd,"*I20.0*")
drop if strmatch(cyzd,"*I21*")
drop if strmatch(cyzd,"*I21.*")
drop if strmatch(cyzd,"*I22*")
drop if strmatch(cyzd,"*I22.*")
drop if strmatch(cyzd,"*I20.0*")
drop if strmatch(cyzd,"*I21*")
drop if strmatch(cyzd,"*I21.*")
drop if strmatch(cyzd,"*I22*")
drop if strmatch(cyzd,"*I22.*")
gen b=0
replace b=1 if strmatch(cyzd,"*I25.2*")
replace b=1 if strmatch(cyzdms,"*陈旧心*")
replace b=1 if strmatch(cyzdms,"*陈旧ST*")
replace b=1 if strmatch(cyzdms,"*陈旧非ST*")
replace b=1 if strmatch(cyzdms,"*陈旧st*")
replace b=1 if strmatch(cyzdms,"*陈旧非st*")
replace b=1 if strmatch(cyzdms,"*陈旧Q*")
replace b=1 if strmatch(cyzdms,"*陈旧q*")
replace b=1 if strmatch(cyzdms,"*陈旧前*")
replace b=1 if strmatch(cyzdms,"*陈旧侧*")
replace b=1 if strmatch(cyzdms,"*陈旧下*")
replace b=1 if strmatch(cyzdms,"*陈旧高*")
replace b=1 if strmatch(cyzdms,"*陈旧间*")
replace b=1 if strmatch(cyzdms,"*陈旧广泛*")
replace b=1 if strmatch(cyzdms,"*陈旧左心室*")
replace b=1 if strmatch(cyzdms,"*陈旧右心室*")
replace b=1 if strmatch(cyzdms,"*陈旧性心*")
replace b=1 if strmatch(cyzdms,"*陈旧性ST*")
replace b=1 if strmatch(cyzdms,"*陈旧性非ST*")
replace b=1 if strmatch(cyzdms,"*陈旧性st*")
replace b=1 if strmatch(cyzdms,"*陈旧性非st*")
replace b=1 if strmatch(cyzdms,"*陈旧性Q*")
replace b=1 if strmatch(cyzdms,"*陈旧性q*")
replace b=1 if strmatch(cyzdms,"*陈旧性前*")
replace b=1 if strmatch(cyzdms,"*陈旧性侧*")
replace b=1 if strmatch(cyzdms,"*陈旧性下*")
replace b=1 if strmatch(cyzdms,"*陈旧性高*")
replace b=1 if strmatch(cyzdms,"*陈旧性间*")
replace b=1 if strmatch(cyzdms,"*陈旧性广泛*")
replace b=1 if strmatch(cyzdms,"*陈旧性左心室*")
replace b=1 if strmatch(cyzdms,"*陈旧性右心室*")
replace b=1 if strmatch(cyzdms,"*陈旧型心*")
replace b=1 if strmatch(cyzdms,"*陈旧型ST*")
replace b=1 if strmatch(cyzdms,"*陈旧型非ST*")
replace b=1 if strmatch(cyzdms,"*陈旧型st*")
replace b=1 if strmatch(cyzdms,"*陈旧型非st*")
replace b=1 if strmatch(cyzdms,"*陈旧型Q*")
replace b=1 if strmatch(cyzdms,"*陈旧型q*")
replace b=1 if strmatch(cyzdms,"*陈旧型前*")
replace b=1 if strmatch(cyzdms,"*陈旧型侧*")
replace b=1 if strmatch(cyzdms,"*陈旧型下*")
replace b=1 if strmatch(cyzdms,"*陈旧型高*")
replace b=1 if strmatch(cyzdms,"*陈旧型间*")
replace b=1 if strmatch(cyzdms,"*陈旧型广泛*")
replace b=1 if strmatch(cyzdms,"*陈旧型左心室*")
replace b=1 if strmatch(cyzdms,"*陈旧型右心室*")
replace b=1 if strmatch(cyzdmc,"*陈旧心*")
replace b=1 if strmatch(cyzdmc,"*陈旧ST*")
replace b=1 if strmatch(cyzdmc,"*陈旧非ST*")
replace b=1 if strmatch(cyzdmc,"*陈旧st*")
replace b=1 if strmatch(cyzdmc,"*陈旧非st*")
replace b=1 if strmatch(cyzdmc,"*陈旧Q*")
replace b=1 if strmatch(cyzdmc,"*陈旧q*")
replace b=1 if strmatch(cyzdmc,"*陈旧前*")
replace b=1 if strmatch(cyzdmc,"*陈旧侧*")
replace b=1 if strmatch(cyzdmc,"*陈旧下*")
replace b=1 if strmatch(cyzdmc,"*陈旧高*")
replace b=1 if strmatch(cyzdmc,"*陈旧间*")
replace b=1 if strmatch(cyzdmc,"*陈旧广泛*")
replace b=1 if strmatch(cyzdmc,"*陈旧左心室*")
replace b=1 if strmatch(cyzdmc,"*陈旧右心室*")
replace b=1 if strmatch(cyzdmc,"*陈旧性心*")
replace b=1 if strmatch(cyzdmc,"*陈旧性ST*")
replace b=1 if strmatch(cyzdmc,"*陈旧性非ST*")
replace b=1 if strmatch(cyzdmc,"*陈旧性st*")
replace b=1 if strmatch(cyzdmc,"*陈旧性非st*")
replace b=1 if strmatch(cyzdmc,"*陈旧性Q*")
replace b=1 if strmatch(cyzdmc,"*陈旧性q*")
replace b=1 if strmatch(cyzdmc,"*陈旧性前*")
replace b=1 if strmatch(cyzdmc,"*陈旧性侧*")
replace b=1 if strmatch(cyzdmc,"*陈旧性下*")
replace b=1 if strmatch(cyzdmc,"*陈旧性高*")
replace b=1 if strmatch(cyzdmc,"*陈旧性间*")
replace b=1 if strmatch(cyzdmc,"*陈旧性广泛*")
replace b=1 if strmatch(cyzdmc,"*陈旧性左心室*")
replace b=1 if strmatch(cyzdmc,"*陈旧性右心室*")
replace b=1 if strmatch(cyzdmc,"*陈旧型心*")
replace b=1 if strmatch(cyzdmc,"*陈旧型ST*")
replace b=1 if strmatch(cyzdmc,"*陈旧型非ST*")
replace b=1 if strmatch(cyzdmc,"*陈旧型st*")
replace b=1 if strmatch(cyzdmc,"*陈旧型非st*")
replace b=1 if strmatch(cyzdmc,"*陈旧型Q*")
replace b=1 if strmatch(cyzdmc,"*陈旧型q*")
replace b=1 if strmatch(cyzdmc,"*陈旧型前*")
replace b=1 if strmatch(cyzdmc,"*陈旧型侧*")
replace b=1 if strmatch(cyzdmc,"*陈旧型下*")
replace b=1 if strmatch(cyzdmc,"*陈旧型高*")
replace b=1 if strmatch(cyzdmc,"*陈旧型间*")
replace b=1 if strmatch(cyzdmc,"*陈旧型广泛*")
replace b=1 if strmatch(cyzdmc,"*陈旧型左心室*")
replace b=1 if strmatch(cyzdmc,"*陈旧型右心室*")

```

### 排除未造成心肌梗死

```stata
keep if strmatch(cyzdmc,"*未造成心肌梗死*")
```

